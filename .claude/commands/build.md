# /build — Build Widget

Run the production build and report results.

1. Run `npm run build`
2. If the build fails, read the error output and suggest fixes
3. If the build succeeds, report the output files and their sizes
