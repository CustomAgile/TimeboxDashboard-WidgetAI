# /deploy — Deploy to Rally

Build and deploy the widget to Rally as a Custom View.

1. Run `npm run deploy`
2. If deployment fails, read the error and suggest fixes
3. If successful, print the Rally Custom View URL so the user can click it to see their widget
